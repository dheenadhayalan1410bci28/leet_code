static int getCommon(int* nums1, int n1, int* nums2, int n2) {
    for(int p1=0, p2=0; p1<n1 && p2<n2; ){
        int x=nums1[p1], y=nums2[p2];
        if (x==y) return x;
        else if (x>y) p2++;
        else p1++;
    }
    return -1;
}
